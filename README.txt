feture_extractor.py - 
Extracts a vector of fetures out of a given tweet (string).

knn.py - 
Knn classifier. main methods - fit(X,y), predict(x), predict_set(X), accuracy(X, y).

common_word.py - 
Counts the n most common words used in each tweet.

classifier.py -
Trains the model and classify tweets using the classify method.
